# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/trackzon
DATA_STORE=postgres
# For MongoDB Atlas mode:
# DATA_STORE=mongodb
# MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxx.mongodb.net/?retryWrites=true&w=majority
# MONGODB_DB_NAME=trackzon

# Auth Configuration
JWT_SECRET=replace-with-a-long-random-secret
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Email Configuration for Price Drop Notifications
# Gmail Example (recommended for testing):
EMAIL_SERVICE=gmail
EMAIL_USER=illuriabhi8247@gmail.com
EMAIL_PASSWORD=vxjm zduz tbgr ploo

# For Gmail, you need to:
# 1. Enable 2-Factor Authentication on your Google Account
# 2. Generate an "App Password" at https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password here (not your regular Gmail password)

# Default notification email (if user email not available)
DEFAULT_NOTIFICATION_EMAIL=illuriabhi8247@example.com

# Server Configuration
PORT=5001
NODE_ENV=development

# Session Secret (generate a random string)
SESSION_SECRET=your-super-secret-session-key-change-this

# Price Checker Configuration
# The scheduler runs every minute by default
# You can modify this in server/services/priceChecker.ts
