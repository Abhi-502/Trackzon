## Packages
framer-motion | Page transitions and scroll-triggered animations
recharts | Dashboard analytics charts and data visualization for price history
date-fns | Human-readable date formatting

## Notes
The application uses Replit Auth. Authentication is handled via the /api/login and /api/logout routes.
The `@shared/routes` exposes the API contract including Zod schemas and the `buildUrl` helper.
